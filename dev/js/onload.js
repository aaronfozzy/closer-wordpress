$(function() {


	// uniform
	// $(":radio, :checkbox").uniform();
	// Style all <select> elements
	// $("select").uniform();
	// Style everything
	// $("select, input, a.button, button").uniform();

	// accordion
	// nfh_accordion(-50);

});